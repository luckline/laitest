(function(){const data=window.LUCKLINE_LIBRARY||{items:[]},items=data.items,categories=["全部",...new Set(items.map(i=>i.category))],state={category:"全部",query:""},tabs=document.getElementById("libraryTabs"),grid=document.getElementById("resourceGrid"),featuredGrid=document.getElementById("featuredGrid"),featuredSection=document.getElementById("featuredSection"),summary=document.getElementById("resultSummary"),empty=document.getElementById("emptyState"),search=document.getElementById("librarySearch");document.getElementById("resourceCount").textContent=items.length;function esc(v){return v.replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c])}function card(item,featured){const a=document.createElement("a"),article=document.createElement("article");article.className=featured?"resource-card featured":"resource-card";a.href=item.url;a.target="_blank";a.rel="noopener noreferrer";a.dataset.event="open_library_resource";a.innerHTML=`<span class="resource-meta"><i>${item.category}</i><em>${item.domain}</em></span><b>${esc(item.title)}</b><p>${item.note}</p><strong>打开资源 <span aria-hidden="true">↗</span></strong>`;article.appendChild(a);return article}function render(){const q=state.query.toLowerCase(),filtered=items.filter(i=>(state.category==="全部"||i.category===state.category)&&(!q||`${i.title} ${i.domain} ${i.category}`.toLowerCase().includes(q)));grid.replaceChildren(...filtered.map(i=>card(i,false)));summary.textContent=`显示 ${filtered.length} / ${items.length} 条`;empty.hidden=filtered.length>0;featuredSection.hidden=Boolean(q)||state.category!=="全部";tabs.querySelectorAll("button").forEach(b=>b.setAttribute("aria-pressed",String(b.dataset.category===state.category)))}categories.forEach(category=>{const b=document.createElement("button");b.type="button";b.dataset.category=category;b.textContent=category;b.addEventListener("click",()=>{state.category=category;render()});tabs.appendChild(b)});featuredGrid.replaceChildren(...items.filter(i=>i.featured).slice(0,6).map(i=>card(i,true)));search.addEventListener("input",()=>{state.query=search.value.trim();render()});document.addEventListener("keydown",e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();search.focus()}if(e.key==="Escape"&&document.activeElement===search){search.value="";state.query="";search.blur();render()}});document.getElementById("randomLink").addEventListener("click",()=>{const item=items[Math.floor(Math.random()*items.length)];if(item)window.open(item.url,"_blank","noopener,noreferrer")});render()})();
